policy "filter addressed tracks" {
  phase normalize {
    order tracks [video, audio, subtitle] where language in ["eng"] and not commentary
    defaults audio where language in ["eng"] and not commentary
    defaults subtitle where forced
  }
}
