policy "language cleanup" {
  phase normalize {
    container mkv
    keep audio where language in ["eng", "und"]
    keep subtitle where language in ["eng"]
    order tracks [video, audio, subtitle]
    defaults audio where language in ["eng"]
    defaults subtitle where forced
  }
}
